import { IHttpOperation } from '@stoplight/types';

/**
 * Operation with minimum information
 */
export const operation: IHttpOperation = {
  id: '?http-operation-id?',
  method: 'GET',
  path: '/todos',
  responses: [],
};
